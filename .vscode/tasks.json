{
    "tasks": [
        {
            "type": "shell",
            "label": "af-testing",
            "command": "make",
            "group": "test",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}